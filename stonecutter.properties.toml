# Centralized per-version properties. Top-level keys are shared across all nodes; each [version]
# section overrides for that Minecraft build. Single node for now; add sections to backport
# (same path Shulker Pocket took). See https://stonecutter.kikugie.dev/wiki/config/properties
mod.id = "fallow"
mod.version = "0.2.0"
mod.modrinth_id = "fallow"

deps.fabric_loader = "0.19.3"
loomx.loom_version = "1.16-SNAPSHOT"

["26.1.2"]
mod.mc_compat = "~26.1.2"
mod.mc_releases = ["26.1", "26.1.1", "26.1.2"]
deps.fabric_api = "0.151.0+26.1.2"
deps.modmenu = "18.0.0-beta.1"
